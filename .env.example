# 后端端口（本地开发）
PORT=3001
NODE_ENV=development

# P1：外部托管 Postgres（Neon）连接串，带 sslmode=require
# 未配置时应用回退到内存实现（仅开发/测试用，重启即失）
DATABASE_URL=

# --- P2 起使用 ---
# LLM_API_KEY=         # 统一 LLM 抽象密钥
# LLM_BASE_URL=        # 可选，兼容 OpenAI 协议网关
# APP_BASE_URL=        # 部署后的公网地址，用于生成分享链接
